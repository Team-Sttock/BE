spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sttock?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: qwer1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
        highlight_sql: true
        use_sql_comments: true
    defer-datasource-initialization: true
    show-sql: true
  redis: # Redis 접속 정보
    host: localhost
    port: 6379


springdoc:
  api-docs:
    path: /api-docs

logging:
  level:
    org:
      springframework:
        security: DEBUG

swagger:
  api: http://localhost:8080
